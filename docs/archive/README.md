# Archived specs

Moved here 2026-08-16. **Superseded on direction and layout, not deleted** —
each records where a spec turned out to be wrong against real data, and in this
project that is the most useful thing a document does.

| file | superseded by | what still stands |
|---|---|---|
| `ANALYSIS_SPEC.md` | the build itself | The **five corrections** in its status block, each caught by running against real data rather than reasoning about it. Its four-tab layout, k-means clustering and two-FLEX roster are all historical — k-means was retired for topping out at a silhouette of 0.29, and the league went superflex in 2026. |
| `DASHBOARD_SPEC.md` | `RESEARCH_SPEC.md` | Why the six tabs existed, and the "what actually shipped" section. |
| `DASHBOARD_SPEC_v2.md` | `RESEARCH_SPEC.md` | Its **"what actually shipped"** section, including two things that spec got wrong. Also the call that the honest negative results are the most credible thing in the app — that decision stands. |

**Read these before re-proposing something they declined.** A decision that was
made once with evidence should not be re-litigated from scratch, and the reason
each was declined is written down.

Current docs live at the repo root — start with `DRAFT_CHECKLIST.md`.
